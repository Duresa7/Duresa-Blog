# Blog Website

Standalone Astro static blog. Posts live in `src/lib/posts.ts`.

## Develop

```
npm install
npm run dev
```

## Build

```
npm run build
```

Outputs static HTML to `dist/`. Deploy via any static host (Coolify Nixpacks static, Cloudflare Pages, Netlify, etc.).
